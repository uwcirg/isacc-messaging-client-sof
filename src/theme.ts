import {createTheme} from "@mui/material/styles";


export const theme = createTheme({
    palette: {
        primary: {
            main: '#4b2e83'
        },
        secondary: {
            main: "#b7a57a"
        },
        divider: "rgba(0,0,0,0.12)"
    },
    typography: {
        button: {},
    }
});